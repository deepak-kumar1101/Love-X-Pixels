/**
 * Firebase has been fully removed from this project.
 * All data persistence and storage now runs on Supabase.
 *
 * This stub exists only to satisfy any lingering import paths
 * during the transition — they can be safely deleted once confirmed clean.
 */

export const isFirebaseConfigured = false;
export const app = null;
export const auth = null;
export const db = null;
export const storage = null;
export const analytics = null;
export const appCheck = null;
